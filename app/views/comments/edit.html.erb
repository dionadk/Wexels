<h2>Add Comment</h2>

<%= form_for [@pic,@comment] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>

<%= form_for [@pic,@comment], method: :delete do |f| %>
  <%= f.submit "Delete!" %>
<% end %>
